import { Meta, StoryObj } from '@storybook/react';
import { Eskeleton } from '.';

export default {
  title: 'components/Eskeleto',
  component: Eskeleton,
} as Meta;

export const Default: StoryObj = {};
