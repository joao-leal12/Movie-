import { Meta, StoryObj } from '@storybook/react';
import { StarAverage } from '.';

export default {
  title: 'Components/StarAverage',
  component: StarAverage,
} as Meta;

export const Default: StoryObj = {};
