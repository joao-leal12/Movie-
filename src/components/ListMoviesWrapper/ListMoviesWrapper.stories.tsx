import { Meta, StoryObj } from '@storybook/react';
import { ListMoviesWrapper } from '.';

export default {
  title: 'components/ListMoviesWrapper',
  component: ListMoviesWrapper,
} as Meta;

export const Default: StoryObj = {};
