import { chakra } from '@chakra-ui/react';

export const SidebarWrapperLink = chakra('ul', {
  baseStyle: {
    display: 'flex',
    flexDirection: 'column',
    paddingInline: '2.4rem',
    borderRadius: '50%',
  },
});
