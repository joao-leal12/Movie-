import { Meta, StoryObj } from '@storybook/react';
import { ToogleColor } from '.';

export default {
  title: 'Components/ToogleColor',
  component: ToogleColor,
} as Meta;

export const Default: StoryObj = {};
